#!/bin/bash
#перевірка аргумента
[ "$#" -ne 1 ] && echo "Використання: $0 <файл>" && exit 1
#перевірка існування
if [ -f "$1" ]; then
    #якщо файл є
    cat "$1"
else
    #файлу немає
    echo "Помилка: файл не існує"
fi