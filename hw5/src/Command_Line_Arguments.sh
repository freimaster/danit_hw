#!/bin/bash
#чи передано аргумент
[ "$#" -ne 1 ] && echo "Використання: $0 <файл>" && exit 1
#чи існує файл
[ ! -f "$1" ] && echo "Помилка: файл не існує" && exit 1
#
lines=$(wc -l < "$1")
#+1 якщо в кінці нема переносу каретки
[ -n "$(tail -c1 "$1")" ] && lines=$((lines+1))
echo "$lines"